<p id="notice"><%= notice %></p>
<p>
  <strong>First name:</strong>
  <%= @user.first_name %>
</p>

<p>
  <strong>Last name:</strong>
  <%= @user.last_name %>
</p>

<p>
  <strong>Dob:</strong>
  <%= @user.dob %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
<%= link_to 'New Comment', new_blog_comment_path(@user)%>


<table class="table">
  <thead>
    <tr>
      <th>Blogs</th>
      <th>Comments</th>
<th colspan="3"></th>
</tr>
</thead>

<tbody>
<% @user.blogs.each do |blog| %>
<%= link_to blog do %>
<tr>

<td><%= blog.title %></td>
<% end %>
<% end %>

 <% @user.comments.each do |comment| %>
 <td class = "list-group-item">
   <%= link_to edit_blog_comment_path(@user,comment) do %>
 <%= comment.body %>
  <% end %>
</td>
  <% end %>
</tr>
</tbody>
</table>
